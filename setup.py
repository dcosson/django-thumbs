#!/usr/bin/env python

from distutils.core import setup

setup(name='django-thumbs',
      description = "Overwrites Model's ImageField to handle processing of thumbs.  Find it at http://github.com/dcosson/django-thumbs"
      version='0.2',
      packages=['thumbs'],
      )